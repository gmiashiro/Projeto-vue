# Arquivos gerados pelo sistema e logs
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Diretório de dependências.
# Este é o mais importante. O comando `npm install` irá recriar esta pasta em qualquer computador.
/node_modules

# Diretório de build. Contém o código final para produção, gerado com `npm run build`.
# Não é código-fonte, então não deve ser versionado.
/dist
/dist-ssr

# Arquivos de variáveis de ambiente locais.
# MUITO IMPORTANTE: Estes arquivos podem conter senhas e chaves de API.
# NUNCA suba esses arquivos para um repositório público.
.env.local
.env.*.local

# Cache de ferramentas e editores
.vite/
.idea/
.vscode/